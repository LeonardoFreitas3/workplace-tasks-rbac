{
  "Logging": {
    "LogLevel": {
      "Default": "Information", 
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*",

  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=workplacetasks;Username=postgres;Password=1234" // PostgreSQL connection string used by EF Core
  },

  "JwtSettings": {
    "SecretKey": "workplacetasks_super_secret_key_very_secure_1234", // Symmetric key used to sign JWT tokens
    "Issuer": "WorkplaceTasks", // Identifies who issued the token
    "Audience": "WorkplaceTasksUsers", // Identifies who the token is intended for
    "ExpiryMinutes": 60 // Token expiration time (in minutes)
  }
}
